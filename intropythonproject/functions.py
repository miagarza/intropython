#creates dice simulator using a function
#functions is a block of code or series of actions that can be reused,
# it actually rolls the dice instead of having to write new lines of code
#We usually use variables to store data: Numbers, words, booleans
#functions store reusable blocks of code


#parameters: values we want to give to functions
#import random

#def random_number():  #declaring a function
    #print("Rolling the Dice!")
    #rand_num = random.randint(1, 6)
    #print(rand_num)
#^ copied from random_numbers file

#random_number()
#function that takes no parameters
def print_hello():
    print("Hello!")


x=10
#parameters: give the function values that we have
def name(x):
    x=x+10
    print(x)
    print(x/10)

name(40)

#return value: Allows the function to give a value
#Pulls info out of the function after running code

def generate ran()
    rand= random.randint(1,10)
    return rand
#When we "return" something, we are giving the func that value
def string_to_int(word):
    return int(word)
x=string_to_int("10")

print(generate_ran())
